# Flask IA service environment variables

# Database (match backend PostgreSQL credentials)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=greenhouse
DB_USER=postgres
DB_PASS=postgres

# Flask server
FLASK_PORT=5000
FLASK_DEBUG=false
