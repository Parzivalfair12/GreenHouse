# Backend API URL (sin proxy). Vacío para local con proxy de Vite.
# En produccion: https://greenhouse-api.up.railway.app
VITE_API_URL=

# Google OAuth URL base (se construye automaticamente desde VITE_API_URL si esta definida)
# En produccion se usa VITE_API_URL + /oauth2/authorization/google
